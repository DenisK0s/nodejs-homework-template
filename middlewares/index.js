const authenticate = require("./authenticate");
const upload = require("./upload");
const avatarSizeCorrector = require("./avatarSizeCorrector");

module.exports = { authenticate, upload, avatarSizeCorrector };
